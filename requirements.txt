beautifulsoup4==4.12.2
lxml>=4.9.0
pandas>=2.2.0
spacy>=3.7.2
huspacy>=0.4.0
# HuSpaCy is compatible with spaCy 3.x and provides Hungarian models
# After installing, run: python -c "import huspacy; huspacy.download()"